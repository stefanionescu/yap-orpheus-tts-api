# Optional extras for TRT-LLM backend (install via scripts/01-install-trt.sh)
mpi4py>=3.1
hf_transfer>=0.1.6
soundfile>=0.12.1
ninja
packaging
setuptools
wheel
numpy>=1.26
transformers>=4.44.2
snac>=0.1.6

# NOTE: tensorrt-llm itself is installed from NVIDIA PyPI wheel (Linux only):
#   pip install https://pypi.nvidia.com/tensorrt-llm/tensorrt_llm-0.20.0-cp310-cp310-linux_x86_64.whl

